# AtalhosIntellij
Conjunto de atalhos para o IntelliJ

# Atalhos e Abreviações para Produtividade no IntelliJ IDEA

Este documento contém uma lista de **atalhos de teclado** e **abreviações de digitação (live templates)** para o IntelliJ IDEA, com foco em Java 17. Use essas ferramentas para aumentar sua produtividade ao codar.

---

## **Abreviações de Digitação (Live Templates)**

Essas abreviações são expandidas automaticamente ao pressionar `Tab` ou `Enter`:

1. **`sout`**  
   Expande para:  
   ```java
   System.out.println();
soutv

Expande para:



System.out.println("var = " + var);
soutp

Expande para:



System.out.println("param = " + param);
psvm

Expande para:



public static void main(String[] args) {
}
fori

Expande para:



for (int i = 0; i < ; i++) {
}
iter

Expande para:



for (Type item : collection) {
}
itco

Expande para:



for (Type item : collection) {
    if (item != null) {
    }
}
ifn

Expande para:



if (var == null) {
}
inn

Expande para:



if (var != null) {
}
switch (Java 17 - Switch Expressions)

Expande para:



switch (variable) {
    case VALUE1 -> {
    }
    case VALUE2 -> {
    }
    default -> {
    }
}
Atalhos de Teclado (Ctrl+...)
Ctrl+Space Sugestão de código (autocompletar).
Ctrl+Shift+Space Sugestão inteligente de código.
Ctrl+Alt+L Reformatar código.
Ctrl+Shift+Enter Completar a linha de código automaticamente.
Ctrl+Q Mostrar documentação rápida.
Ctrl+P Mostrar parâmetros do método.
Ctrl+Shift+F10 Executar o programa atual.
Ctrl+D Duplicar a linha ou seleção.
Ctrl+Y Excluir a linha atual.
Ctrl+/ Comentar/descomentar linha.
Ctrl+Shift+/ Comentar/descomentar bloco.
Ctrl+F Localizar no arquivo.
Ctrl+R Substituir no arquivo.
Ctrl+Shift+F Localizar em todo o projeto.
Ctrl+Shift+R Substituir em todo o projeto.
Ctrl+E Mostrar arquivos recentes.
Ctrl+Shift+T Criar ou navegar para o teste de unidade.
Ctrl+Alt+O Otimizar imports.
Ctrl+Shift+N Localizar arquivo no projeto.
Ctrl+Alt+V Extrair variável.
Ctrl+Alt+M Extrair método.
Ctrl+Alt+C Extrair constante.
Ctrl+Alt+F Extrair campo.
Ctrl+Shift+U Alternar entre maiúsculas e minúsculas.
